enum Role {
    CUSTOMER
    ADMIN
    PROVIDER
}

enum Status {
    ACTIVE
    SUSPENDED
}

enum OrderStatus {
    PLACED
    PENDING
    CONFIRMED
    PAID
    PICKED_UP
    RETURNED
    CANCELLED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentMethod {
  CARD
  MOBILE_BANKING
  BANK_TRANSFER
  CASH
}

enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}